# Database configuration
db.url=jdbc:postgresql://localhost:5432/publication_system
db.username=postgres
db.password=admin
db.driver=org.postgresql.Driver

# JWT configuration
jwt.secret=your_jwt_secret_key
jwt.expiration=3600 # 1 hour expiration
