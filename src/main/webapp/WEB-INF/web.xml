<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://java.sun.com/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://java.sun.com/xml/ns/javaee
             http://java.sun.com/xml/ns/javaee/web-app_3_0.xsd"
         version="3.0">

    <!-- Сервлети для керування HTTP-запитами -->
    <servlet>
        <servlet-name>AdminServlet</servlet-name>
        <servlet-class>org.app.controller.AdminServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>

    <servlet>
        <servlet-name>SubscriptionServlet</servlet-name>
        <servlet-class>org.app.controller.SubscriptionServlet</servlet-class>
        <load-on-startup>2</load-on-startup>
    </servlet>

    <servlet>
        <servlet-name>PaymentServlet</servlet-name>
        <servlet-class>org.app.controller.PaymentServlet</servlet-class>
        <load-on-startup>3</load-on-startup>
    </servlet>

    <!-- Фільтри для обробки запитів, таких як перевірка JWT -->
    <filter>
        <filter-name>JWTAuthFilter</filter-name>
        <filter-class>org.app.filter.JWTAuthFilter</filter-class>
    </filter>

    <filter-mapping>
        <filter-name>JWTAuthFilter</filter-name>
        <url-pattern>/*</url-pattern> <!-- Фільтруємо всі запити -->
    </filter-mapping>

    <!-- Welcome файл для кореневого контексту -->
    <welcome-file-list>
        <welcome-file>index.html</welcome-file>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <!-- Сесії та конфігурація контексту -->
    <session-config>
        <session-timeout>30</session-timeout> <!-- Час сесії: 30 хвилин -->
    </session-config>

    <!-- Маппінг для сервлетів -->
    <servlet-mapping>
        <servlet-name>AdminServlet</servlet-name>
        <url-pattern>/admin/*</url-pattern>
    </servlet-mapping>

    <servlet-mapping>
        <servlet-name>SubscriptionServlet</servlet-name>
        <url-pattern>/subscription/*</url-pattern>
    </servlet-mapping>

    <servlet-mapping>
        <servlet-name>PaymentServlet</servlet-name>
        <url-pattern>/payment/*</url-pattern>
    </servlet-mapping>

    <!-- Конфігурація для файлів статичного контенту (можна змінювати відповідно до потреб) -->
    <servlet>
        <servlet-name>StaticResourceServlet</servlet-name>
        <servlet-class>org.apache.catalina.servlets.DefaultServlet</servlet-class>
        <init-param>
            <param-name>debug</param-name>
            <param-value>0</param-value>
        </init-param>
        <init-param>
            <param-name>listings</param-name>
            <param-value>false</param-value>
        </init-param>
    </servlet>

    <servlet-mapping>
        <servlet-name>StaticResourceServlet</servlet-name>
        <url-pattern>/static/*</url-pattern>
    </servlet-mapping>

</web-app>
